# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Task API
    version: 0.0.1
paths:
    /task/get-all-http-cases:
        post:
            tags:
                - Task
            operationId: Task_GetAllHttpTaskCases
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CommonRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetAllHttpTaskCasesReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /task/run-http-task:
        post:
            tags:
                - Task
            operationId: Task_RunHttpTask
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RunHttpTaskRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RunHttpTaskReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        CommonRequest:
            type: object
            properties: {}
        GetAllHttpTaskCasesReply:
            type: object
            properties:
                httpTaskCases:
                    type: array
                    items:
                        $ref: '#/components/schemas/HttpTaskCase'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        HttpRequest:
            type: object
            properties:
                method:
                    type: string
                url:
                    type: string
                headers:
                    type: object
                    additionalProperties:
                        type: string
                params:
                    type: object
                    additionalProperties:
                        type: string
            description: HttpRequest represent the structure of real request
        HttpResponse:
            type: object
            properties:
                status:
                    type: integer
                    format: int32
                headers:
                    type: object
                    additionalProperties:
                        type: string
                body:
                    type: string
                delay:
                    $ref: '#/components/schemas/ResponseDelay'
            description: HttpResponse represent the structure of real response
        HttpTaskCase:
            type: object
            properties:
                id:
                    type: string
                Request:
                    $ref: '#/components/schemas/HttpRequest'
                Response:
                    $ref: '#/components/schemas/HttpResponse'
            description: '----------------------------------------------------------------'
        ResponseDelay:
            type: object
            properties:
                delay:
                    type: integer
                    format: int64
                offset:
                    type: integer
                    format: int64
            description: ResponseDelay represent time delay before server responds.
        RunHttpTaskReply:
            type: object
            properties: {}
        RunHttpTaskRequest:
            type: object
            properties: {}
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Task
